name: Build & Release (Windows)

on:
  push:
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.14"]
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python-version }}
          architecture: x64

      - name: Install test dependencies
        run: python -m pip install --disable-pip-version-check --require-hashes -r requirements-win.lock

      - name: Run tests
        env:
          QT_QPA_PLATFORM: offscreen
        run: python -B -m unittest discover -s tests -v

      - name: Verify dependency consistency
        run: python -m pip check

      - name: Audit runtime dependencies
        run: python -m pip_audit -r requirements-win.lock --strict

  build-windows:
    needs: test
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Verify APP_VERSION matches the tag
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          set -euo pipefail
          tag_version="${GITHUB_REF_NAME#v}"
          app_version="$(awk -F\" '/APP_VERSION *=/ {print $2}' version.py)"
          test -n "$app_version"
          test "$tag_version" = "$app_version"

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"
          architecture: x64

      - name: Install build dependencies
        run: python -m pip install --disable-pip-version-check --require-hashes -r requirements-win.lock

      - name: Build executable
        run: pyinstaller --noconfirm --clean DIM-Creator.spec

      - name: Smoke-test executable
        run: .\dist\DIM-Creator.exe --smoke-test

      - name: Add documentation
        shell: pwsh
        run: |
          Copy-Item README.md dist/README.md -Force
          Copy-Item LICENSE dist/LICENSE -Force

      - name: Generate SBOM
        run: python -m pip_audit -r requirements-win.lock --format cyclonedx-json --output dist/sbom.cdx.json

      - name: Package release ZIP
        shell: pwsh
        run: |
          $version = if ($env:GITHUB_REF_NAME) { $env:GITHUB_REF_NAME } else { "dev" }
          $version = $version -replace '[^0-9A-Za-z._-]', '_'
          $archive = "dist/DIM-Creator_${version}_win-x64.zip"
          Compress-Archive -Path dist/DIM-Creator.exe,dist/README.md,dist/LICENSE,dist/sbom.cdx.json -DestinationPath $archive -Force

      - name: Generate SHA-256 checksums
        shell: pwsh
        run: |
          Get-ChildItem dist -File | Where-Object { $_.Extension -in '.exe', '.zip', '.json' } |
            Sort-Object Name | ForEach-Object {
              $hash = (Get-FileHash $_.FullName -Algorithm SHA256).Hash.ToLowerInvariant()
              "$hash  $($_.Name)"
            } | Set-Content dist/SHA256SUMS.txt -Encoding ascii

      - name: Upload build artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: dim-creator-win-x64
          path: |
            dist/DIM-Creator.exe
            dist/DIM-Creator_*_win-x64.zip
            dist/sbom.cdx.json
            dist/SHA256SUMS.txt
            dist/README.md
            dist/LICENSE
          if-no-files-found: error
          retention-days: 14

  publish:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build-windows
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout release notes
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Download build artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dim-creator-win-x64
          path: dist

      - name: Extract changelog section
        id: changelog
        shell: bash
        run: |
          extract_section() {
            awk -v heading="$1" '
            /^[[:space:]]*##[[:space:]]+/ {
              if (found) exit
              section=$0
              sub(/^[[:space:]]*##[[:space:]]+/, "", section)
              found=(section == heading ||
                (index(section, heading) == 1 &&
                 substr(section, length(heading) + 1, 1) ~ /[[:space:]]/))
            }
            found { print }
            ' CHANGELOG.md
          }

          notes="$(extract_section "$GITHUB_REF_NAME")"
          if [[ -z "$notes" ]]; then
            notes="$(extract_section "Unreleased")"
          fi
          {
            echo 'notes<<EOF'
            echo "$notes"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Publish GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_NOTES: ${{ steps.changelog.outputs.notes }}
        run: |
          set -euo pipefail
          assets=(
            dist/DIM-Creator.exe
            dist/DIM-Creator_*_win-x64.zip
            dist/sbom.cdx.json
            dist/SHA256SUMS.txt
          )

          if [[ -n "$RELEASE_NOTES" ]]; then
            notes_file="$RUNNER_TEMP/release-notes.md"
            printf '%s\n' "$RELEASE_NOTES" > "$notes_file"
            gh release create "$GITHUB_REF_NAME" \
              --verify-tag --notes-file "$notes_file" "${assets[@]}"
          else
            gh release create "$GITHUB_REF_NAME" \
              --verify-tag --generate-notes "${assets[@]}"
          fi
